# AI usage

> Regla de la entrega: la IA puede proponer código, pero no puede decidir silenciosamente el
> contrato o las reglas del sistema. Solo se usa **después** de la marca `class-04-design`.
> No hace falta copiar conversaciones completas: registra lo esencial con honestidad.
> Esta sección se completa en la fase 4, después de implementar y probar contra la base.

## My design before using AI

(Pendiente de completar en fase 4 — el diseño vive en `class-04-design`: `data-model.md`,
`persistence-contract.md`, `query-matrix.md`, `transaction-plan.md`, `error-map.md` y
`test-matrix.md`.)

## What I asked the AI

(Pendiente.)

## What the AI proposed

(Pendiente.)

## What I accepted

(Pendiente.)

## What I rejected or changed

(Pendiente.)

## How I verified the result

(Pendiente.)

## What I still do not understand

(Pendiente.)